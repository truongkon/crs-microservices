# Tên service
spring.application.name=registration-service

# Cổng chạy: 8084 (để tránh đụng độ với course-service đang chạy ở 8083)
server.port=8084

# Kết nối MySQL Database vừa tạo ở Bước 1
spring.datasource.url=jdbc:mysql://localhost:3306/registration_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Ho_Chi_Minh
spring.datasource.username=root
spring.datasource.password=24062005

# Hibernate tự động tạo bảng
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# URL nội bộ trỏ tới course-service
course-service.base-url=http://localhost:8083